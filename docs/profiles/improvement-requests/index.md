---
okf_version: "0.2"
---

# Subdirectories

- [types/](types/index.md)

# OKF Profile

- [cross-repository-improvement-requests](profile.md) - Cross-repository improvement proposals with stable IR handles and review provenance. The house `reviews` family and OKF `verified` coexist: `reviews` records far more than `verified` can, so an approving `reviews` entry should also be mirrored into `verified` to keep the derived trust tier accurate.

