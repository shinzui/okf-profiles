# Bundle Update Log

## 2026-08-13

* **Addition**: REV-1 demonstrates a full model review of a subject whose Mori
  URI names its container, so identity is carried by `subject` plus `component`.
* **Addition**: REV-2 demonstrates the chain REV-1 exists to start — an
  incremental review that names its base commit and its predecessor.
* **Addition**: REV-3 demonstrates a human review, which demands none of the
  provider, model, or effort keys, of a subject whose Mori URI needs no
  `component`.
